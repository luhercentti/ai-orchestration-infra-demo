apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: ai-orchestration-demo
spec:
  selector:
    app: orchestrator
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
